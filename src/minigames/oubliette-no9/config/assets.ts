/**
 * Shared asset URLs. Using a single constant ensures the browser caches
 * the resource once and reuses it across all consumers.
 */
export const LOGO_URL = `${import.meta.env.BASE_URL}images/logos/number9.png`;
